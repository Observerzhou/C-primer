#include <iostream>
#include <string>

using namespace std;

int main()
{
	string  s;
	
	cout << "请输入带标点的字符串" << endl;
	while (getline(cin,s))
		for (auto &i : s)
			if(!ispunct(s))
				cout << i;
		cout << endl;


	return 0;
}
